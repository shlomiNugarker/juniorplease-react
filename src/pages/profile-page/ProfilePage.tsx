// import { useNavigate } from 'react-router-dom'

type Props = {}

export default function ProfilePage(props: Props) {
  // const navigate = useNavigate()

  return <section className="profile-page">profile-page</section>
}
