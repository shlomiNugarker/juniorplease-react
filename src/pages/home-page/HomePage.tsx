// import { useNavigate } from 'react-router-dom'

type Props = {}

export default function HomePage(props: Props) {
  // const navigate = useNavigate()

  return <section className="home-page">HomePage</section>
}
